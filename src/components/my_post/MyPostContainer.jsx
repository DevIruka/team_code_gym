import React from 'react'
import PostCardContainer from '../postCards/PostCardContainer'

const MyPostContainer = () => {
  return <PostCardContainer isHome={false} />
}

export default MyPostContainer
